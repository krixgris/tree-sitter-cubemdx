{
  "name": "tree-sitter-cubemdx",
  "version": "1.0.0",
  "description": "tree-sitter parser for mdx inside a .cube xml file for olap cubes",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mdx",
    "olap",
    "cube",
    "calculations"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.19.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.22.1"
  }
}
